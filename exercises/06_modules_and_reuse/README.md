# Modules and reuse exercises

Implement a function that receives a data-producing scriptblock. The seam makes
the function testable without an external API or global state.

```powershell
pwsh -NoProfile -File exercises/06_modules_and_reuse/solutions.ps1
```
